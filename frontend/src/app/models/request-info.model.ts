export interface RequestInfo {
    requestType: string;
    refNumber: string;
    appSender: string;
    popId: string;
    workstationId: string;
    requestId: string;
    stan: string; // Nouveau champ ajouté
  }
  
